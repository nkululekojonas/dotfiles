[user]
    name = Nkululeko Jonas
	email = 3524042+nkululekojonas@users.noreply.github.com

[core]
	eol = lf
    editor = vim
	autocrlf = false
    excludesfile = ~/.config/git/gitignore
    whitespace = fix,-indent-with-non-tab,trailing-space,cr-at-eol

[alias]
    al = add .
    cl = clone
    ci = commit
    cn = config
    co = checkout
    st = status 
    br = branch
    lg = log --graph --pretty=format:'%Cred%h%Creset -%C(yellow)%d%Creset %s %Cgreen(%cr) %C(bold blue)<%an>%Creset' \
         --abbrev-commit
    ld = log --oneline --decorate --graph --all
    ls = log --stat
    df = diff
    amend = commit --amend
    last = log -1 HEAD
    unstage = reset HEAD --
    ignore = update-index --assume-unchanged
    unignore = update-index --no-assume-unchanged
    changes = diff --name-status -r HEAD

[branch]
    autosetuprebase = always

[merge]
    tool = vimdiff
    conflictstyle = diff3
    ff = only

[diff]
    tool = vimdiff

[push]
    default = simple

[color]
    ui = true
    branch = auto
    diff = auto
    interactive = auto
    status = auto

[pull]
    rebase = true
[fsck]
	zeroPaddedFilemode = ignore
[fetch "fsck"]
	zeroPaddedFilemode = ignore
[receive "fsck"]
	zeroPaddedFilemode = ignore
[oh-my-zsh]
	lastVersion = 1514145a09140425b914e9cd98fc1d638a8c4003
[init]
	defaultBranch = main
